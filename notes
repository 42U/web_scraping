none yet lol... but so many bookmarks and much videos

for using variables in a string
	('hanning' + str(num) + '.pdf')
	'hanning%(num)s.pdf' % locals()
	'hanning{0}.pdf'.format(num)
	f'hanning{0}.pdf'
	string.Template('hanning'${num}.pdf').substitute(locals())

#name a new variable after class has been defined

EZclap.new = "this value is for the new var in ez clap"

#the link to pull apart
f"https://{locale}.craigslist.org/search/mca?min_price={min_price}&max_price={max_price}&auto_make_model={make}{sep}{model}&min_engine_displacement_cc={min_disp}&max_engine_displacement_cc={max_disp}&min_auto_year=2002&max_auto_year=2019&min_auto_miles={min_miles}&max_auto_miles={max_miles}&auto_title_status=1&auto_title_status=5"

if you use the same variable again should set its value to 0 first...
	it will read the end of the list and give no value

